---
description: Type definition for a reward interception route.
---

# Interception

A reward interception route, an asset and the recipients its rewards are split across.
Passed as `initialInterceptions` when spawning a conduit or a multi-vehicle.

## Import

```ts twoslash
import type { Interception } from '@railnetorg/railnet-sdk'
```

## Definition

```ts
type Interception = {
  asset: Address
  recipients: Array<{
    target: Address
    shareBps: bigint
    chainId: bigint
  }>
}
```

## Properties

### asset

- **Type:** `Address`

### recipients

- **Type:** `Array<{ target: Address; shareBps: bigint; chainId: bigint }>`

The recipients the intercepted asset is routed to. `shareBps` is the recipient's share in basis
points, and `chainId` is the chain the `target` lives on.
