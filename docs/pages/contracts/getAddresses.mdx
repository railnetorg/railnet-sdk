---
description: Returns protocol contract addresses for a given chain.
---

# getAddresses

Returns all protocol contract addresses for a supported chain. Throws if the chain is not supported.

## Import

```ts
import { getAddresses } from '@railnetorg/railnet-sdk'
```

## Usage

```ts
import { mainnet } from 'viem/chains'
import { getAddresses } from '@railnetorg/railnet-sdk'

const { conduitFactory, usdc, aaveV3VehicleFactory } = getAddresses(mainnet.id)
```

### With buildSpawnConduitCall

```ts
import { createWalletClient, http } from 'viem'
import { mainnet } from 'viem/chains'
import { buildSpawnConduitCall, getAddresses, randomSalt } from '@railnetorg/railnet-sdk'

const walletClient = createWalletClient({
  chain: mainnet,
  transport: http(),
})

const { conduitFactory } = getAddresses(mainnet.id)

const hash = await walletClient.writeContract({
  ...buildSpawnConduitCall({
    factory: conduitFactory,
    name: 'My Conduit',
    symbol: 'COND',
    vehicle: '0x...',
    initialExpectedSupply: 1000000n,
    transferEnabled: true,
    accessControl: '0x...',
    feeManager: '0x...',
    accountList: '0x...',
    ownerRegistry: '0x...',
    querySalt: randomSalt(),
    deploymentSalt: randomSalt(),
  }),
  account: '0x...',
})
```

## Returns

`ChainAddresses`

An object containing all protocol contract addresses for the chain. See [`ChainAddresses`](/types/ChainAddresses).

## Parameters

### chainId

- **Type:** `number`

The chain ID to look up. Throws an error if the chain is not supported.

## Supported Chains

| Chain | Chain ID |
| --- | --- |
| Ethereum | `1` |

All addresses are defined in [`src/contracts/addresses.ts`](https://github.com/railnetorg/railnet-sdk/blob/main/src/contracts/addresses.ts).

## Error Handling

```ts
import { getAddresses, isSupportedChain } from '@railnetorg/railnet-sdk'

const chainId = 42161

if (isSupportedChain(chainId)) {
  const addresses = getAddresses(chainId)
}
```

## Production only

This entry point holds **production** deployments. Base is not here: it has no production
deployment yet, so `getAddresses(8453)` throws rather than answering with the staging deployment
Base runs today.

Staging lives behind its own entry point — see [Staging deployments](/contracts/staging).
