---
description: Finalizes a conduit deposit after the initial setup.
---

# buildFinalizeConduitDepositCall

Finalizes a conduit deposit after the initial setup. Returns the call to send. Hand it to viem's `simulateContract` then `writeContract`, or to wagmi's `useWriteContract`.

## Import

```ts
import { buildFinalizeConduitDepositCall } from '@railnetorg/railnet-sdk'
```

## Usage

```ts
import { simulateContract, writeContract } from 'viem/actions'
import { buildFinalizeConduitDepositCall } from '@railnetorg/railnet-sdk'

const { request } = await simulateContract(client, {
  ...buildFinalizeConduitDepositCall({
    factory: '0x1234...',
    conduit: '0xA0Cf798816D4b9b9866b5330EEa46a18382f251e',
  }),
  account: '0xd2135CfB216b74109775236E36d4b433F1DF507B',
})

const hash = await writeContract(client, request)
```

## Returns

A prepared call: `{ address, abi, functionName, args }`.

## Parameters

### factory

- **Type:** `Address`

The address of the conduit factory contract.

```ts
buildFinalizeConduitDepositCall({
  factory: '0x1234...', // [!code focus]
  conduit: '0x...',
  account: '0x...',
})
```

### conduit

- **Type:** `Address`

The address of the conduit contract.

```ts
buildFinalizeConduitDepositCall({
  factory: '0x...',
  conduit: '0xA0Cf798816D4b9b9866b5330EEa46a18382f251e', // [!code focus]
  account: '0x...',
})
```

### account

- **Type:** `Address`

The address of the account executing the transaction.

```ts
buildFinalizeConduitDepositCall({
  factory: '0x...',
  conduit: '0x...',
  account: '0xd2135CfB216b74109775236E36d4b433F1DF507B', // [!code focus]
})
```
