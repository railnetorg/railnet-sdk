---
description: Revokes a scoped role on the ExternalAccessControl.
---

# buildRevokeScopedRoleCall

Revokes a scoped role on the ExternalAccessControl contract.

The caller must hold the role's admin role — `DEFAULT_ADMIN_ROLE` unless it was reassigned — either globally or scoped to the same `scope`. Reverts `PublicRoleAuthDenied` when that scoped role is already public: turn it off with [`buildSetScopedRolePublicCall`](/actions/buildSetScopedRolePublicCall) first.

## Import

```ts twoslash
import { buildRevokeScopedRoleCall } from '@railnetorg/railnet-sdk'
```

## Usage

```ts
import { simulateContract, writeContract } from 'viem/actions'
import { buildRevokeScopedRoleCall } from '@railnetorg/railnet-sdk'

const { request } = await simulateContract(client, {
  ...buildRevokeScopedRoleCall({
    accessControl: '0x1234...',
    role: VEHICLE_STEAM_DEPOSIT,
    scope: '0x5678...',
    grantee: '0x9012...',
  }),
  account: '0xd2135CfB216b74109775236E36d4b433F1DF507B',
})

const hash = await writeContract(client, request)
```

## Returns

A prepared call: `{ address, abi, functionName, args }`.

## Parameters

### accessControl

- **Type:** `Address`

The address of the ExternalAccessControl contract.

### role

- **Type:** `Hex`

### scope

- **Type:** `Address`

### grantee

- **Type:** `Address`
