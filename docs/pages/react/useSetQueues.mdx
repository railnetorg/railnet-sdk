---
description: React hook to set deposit and redeem queues.
---

# useSetQueues

React mutation hook for setting deposit and redeem queues on the QueueStrategyEngine.

## Import

```ts
import { useSetQueues } from '@railnetorg/railnet-sdk/react'
```

## Usage

```tsx
import { useSetQueues } from '@railnetorg/railnet-sdk/react'

function SetQueues() {
  const { mutateAsync, isPending } = useSetQueues()

  const handleSetQueues = async () => {
    await mutateAsync({
      queueStrategyEngine: '0x...',
      depositQueue: [],
      redeemQueue: [],
      account: '0x...',
    })
  }

  return (
    <button disabled={isPending} onClick={handleSetQueues}>
      Set Queues
    </button>
  )
}
```

## Parameters

`chainId` — chain to simulate and sign on. Defaults to the connected one.

## Returns

```ts
UseMutationResult<Hash, Error, SetQueuesParameters & { account: Address }>
```

Mutation result. Call `mutate` or `mutateAsync` with [`setQueues`](/actions/setQueues) parameters.

The call is simulated through the transport your wagmi config declares and only the transaction reaches the wallet, so a wallet serving stale state cannot reject a call on state that has already changed.
