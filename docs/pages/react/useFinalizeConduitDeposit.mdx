---
description: React hook to finalize a conduit deposit.
---

# useFinalizeConduitDeposit

React mutation hook for finalizing a conduit deposit via the factory.

## Import

```ts
import { useFinalizeConduitDeposit } from '@railnetorg/railnet-sdk/react'
```

## Usage

```tsx
import { useFinalizeConduitDeposit } from '@railnetorg/railnet-sdk/react'

function Finalize() {
  const { mutate, isPending } = useFinalizeConduitDeposit()

  return (
    <button
      disabled={isPending}
      onClick={() =>
        mutate({
          factory: '0x...',
          conduit: '0x...',
          account: '0x...',
        })
      }
    >
      Finalize Deposit
    </button>
  )
}
```

## Parameters

`chainId` — chain to simulate and sign on. Defaults to the connected one.

## Returns

```ts
UseMutationResult<Hash, Error, FinalizeConduitDepositParameters & { account: Address }>
```

Mutation result. Call `mutate` or `mutateAsync` with [`finalizeConduitDeposit`](/actions/prepareFinalizeConduitDeposit) parameters.

The call is simulated through the transport your wagmi config declares and only the transaction reaches the wallet, so a wallet serving stale state cannot reject a call on state that has already changed.
