---
description: React hook to deposit assets into a conduit.
---

# useDepositConduit

React mutation hook for depositing assets into a conduit.

## Import

```ts
import { useDepositConduit } from '@railnetorg/railnet-sdk/react'
```

## Usage

```tsx
import { useDepositConduit } from '@railnetorg/railnet-sdk/react'

function Deposit() {
  const { mutate, isPending } = useDepositConduit()

  return (
    <button
      disabled={isPending}
      onClick={() =>
        mutate({
          conduit: '0x...',
          token: '0x...',
          amount: 1000000000000000000n,
          account: '0x...',
        })
      }
    >
      Deposit
    </button>
  )
}
```

## Returns

```ts
UseMutationResult<Hash, Error, DepositConduitParameters & { account: Address }>
```

Mutation result. Call `mutate` or `mutateAsync` with [`depositConduit`](/actions/depositConduit) parameters.
