---
description: Query options for estimating a conduit operation.
---

# estimateConduitQueryOptions

Query options for estimating a conduit operation.

## Import

```ts
import { estimateConduitQueryOptions } from '@railnetorg/railnet-sdk/react'
```

## Usage

```ts
import { useQuery } from '@tanstack/react-query'
import { usePublicClient } from 'wagmi'
import { estimateConduitQueryOptions } from '@railnetorg/railnet-sdk/react'
import { ConduitMode, EstimationType } from '@railnetorg/railnet-sdk'

function Estimate() {
  const client = usePublicClient()

  const { data } = useQuery(
    estimateConduitQueryOptions(client, {
      conduit: '0xA0Cf798816D4b9b9866b5330EEa46a18382f251e',
      asset: { asset: '0x...', value: 1000000000000000000n },
      mode: ConduitMode.DEPOSIT,
      estimationType: EstimationType.OUTPUT,
    }),
  )
}
```

## Returns

```ts
QueryOptions<Asset, ReadContractErrorType, Asset, EstimateConduitQueryKey>
```

Options object containing `queryFn` and `queryKey`.

## Parameters

### client

- **Type:** `Client<Transport, Chain | undefined> | undefined`

A viem public client.

### parameters

#### conduit

- **Type:** `Address`

The address of the conduit contract.

#### asset

- **Type:** `Asset`

The input asset for the estimation.

#### mode

- **Type:** `ConduitMode`

The mode for the estimation.

#### estimationType

- **Type:** `EstimationType`

The type of estimation.

## Query Key

```ts
['railnet', 'estimateConduit', { chainId, conduit, asset, mode, estimationType }] as const
```

The hook hands the key back on its result, so the common case needs no builder and no guess about
which chain the client resolved to:

```ts
const { data, queryKey } = useEstimateConduit({ conduit: '0x...' })

queryClient.invalidateQueries({ queryKey })
```

Reach for the builder when the hook is not mounted where you invalidate — a mutation's
`onSuccess` in another component, a route loader.

You can use `estimateConduitQueryKey` to construct the query key manually:

```ts
import { base } from 'viem/chains'
import { estimateConduitQueryKey } from '@railnetorg/railnet-sdk/react'

const queryKey = estimateConduitQueryKey(base.id, parameters)
```

Every read is scoped to a chain, so the key carries the chain it was filled from. The prefix is
exported too, to invalidate the family across every chain and every parameter set:

```ts
import { estimateConduitQueryPrefix } from '@railnetorg/railnet-sdk/react'

queryClient.invalidateQueries({ queryKey: estimateConduitQueryPrefix })
```
