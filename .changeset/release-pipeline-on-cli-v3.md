---
"@railnetorg/railnet-sdk": patch
---

No consumer-facing change. This release exists to exercise the upgraded release pipeline — Changesets CLI 3.0.1 and a pinned `changesets/action` v2.1.1 — end to end, so the path that publishes to partners is proven rather than assumed.
